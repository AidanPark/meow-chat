# meow-chat 환경변수 설정 예시
# 이 파일을 .env로 복사하여 사용하세요

# =============================================================================
# OCR 설정
# =============================================================================

# OCR 제공자 선택: google | easyocr | paddle | dummy
OCR_PROVIDER=easyocr

# OCR GPU 사용 여부 (PaddleOCR / EasyOCR)
# - 로컬 개발: false (CPU 모드)
# - 클라우드 배포: true (GPU 모드)
OCR_USE_GPU=false

# Google Cloud Vision API 사용 시 서비스 계정 JSON 경로
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account.json

# =============================================================================
# LLM 설정
# =============================================================================

# LLM 제공자 선택: openai | anthropic | dummy
LLM_PROVIDER=openai

# OpenAI API 키 (LLM_PROVIDER=openai 시 필수)
# OPENAI_API_KEY=sk-xxx

# Anthropic API 키 (LLM_PROVIDER=anthropic 시 필수)
# ANTHROPIC_API_KEY=sk-ant-xxx

# 모델 설정 (선택)
# OPENAI_MODEL=gpt-4o
# ANTHROPIC_MODEL=claude-3-5-sonnet-20241022

# 오케스트레이션용 모델 설정 (용도별 분리)
# - 의도분류: 빠르고 저렴한 모델 권장
# - 스몰톡: 일반 대화용 중간 품질 모델
# - 검사분석: 고품질 모델 권장
# OPENAI_MODEL_INTENT=gpt-5-nano
# OPENAI_MODEL_CHAT=gpt-5-mini
# OPENAI_MODEL_ANALYSIS=gpt-4.1

# =============================================================================
# 앱 설정
# =============================================================================

# 디버그 모드
APP_DEBUG=false

# 로그 레벨: DEBUG | INFO | WARNING | ERROR
LOG_LEVEL=INFO

# 최대 업로드 크기 (MB)
MAX_UPLOAD_SIZE_MB=10

