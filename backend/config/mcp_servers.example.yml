# Example MCP servers configuration
# Copy this file to `mcp_servers.yml` and adjust per environment.

servers:
  math_utility:
    url: http://localhost:8000/sse
    transport: sse
  weather_api:
    url: http://localhost:8001/sse
    transport: sse
  cat_health:
    url: http://localhost:8002/sse
    transport: sse
