# OCR extras (install only when needed)
# Prefer headless build for server environments
opencv-python-headless>=4.8.0
# PaddleOCR stack (CPU)
# Note: Ensure platform wheels exist for your base image
paddleocr>=2.7.0.3
paddlepaddle>=2.6.0; platform_machine == "x86_64" and platform_system == "Linux"
# If Apple Silicon or other platforms, pin a compatible wheel or use conda-based image instead
